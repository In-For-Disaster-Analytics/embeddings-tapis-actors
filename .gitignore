# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg

# Virtual environments
.venv/
venv/
env/
ENV/

# Env files (never commit real credentials/connection strings)
.env
.env.*
!.env.example

# Test / coverage
.pytest_cache/
.coverage
htmlcov/
.tox/

# Editors / OS
.vscode/
.idea/
.DS_Store

# Large model artifacts -- checkpoints are not committed to this repo (see
# README "Status: skeleton only" -- CLAY_CHECKPOINT_PATH points outside the repo)
*.ckpt
*.pth
*.pt

# OpenWolf / Claude Code local tooling scaffold -- auto-provisioned by the
# environment, not part of this project; never intended to be committed.
.wolf/
.claude/
CLAUDE.md
